
#include "./colors.hpp"

namespace flawed {
    const std::string _flawed_title =            _add_color("[flawed]", _fl_blue);
    const std::string _assertion_failed_string = _add_color("Assertion failed:", _fl_bg_red);
};
