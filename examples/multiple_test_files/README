## Multiple Files Example

This shows how multiple different files can be run together as a target generated by CMake.

Navigate to this folder, run
```
$ cmake -S . -B build
$ cd build
$ make flawed_test
```

to run the tests.